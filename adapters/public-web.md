# Public web adapter archetype

Placeholder namespace: `web:`

A public website or reading interface consumes public Publication-profile projections and versioned protocol resources. It must not serve private records or reconstruct omitted identifiers from lineage, digests, extensions, or build metadata.

The website adapter should expose attribution, omissions, policy status, protocol version, correction route, and revocation route. Downloaded artifacts should be validated after packaging, and unknown extension namespaces should remain hidden unless separately approved for publication.

Documentation search may relate terms across a published corpus, but related terms must retain provenance and non-equivalence notes.
